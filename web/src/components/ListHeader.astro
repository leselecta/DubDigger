---
import { LIST_GRID, LIST_GRID_NO_COUNT } from "./list-grid";

interface Props {
  name: string;
  /** Omitted drops the column entirely, header and rows both. */
  count?: string;
  meta?: string;
}

const { name, count, meta } = Astro.props;
---

<div
  class:list={[
    "border-hairline text-ink-faint grid gap-x-[22px] border-b pb-3 font-mono text-[0.6875rem] tracking-[0.2em] uppercase",
    count ? LIST_GRID : LIST_GRID_NO_COUNT,
  ]}
>
  <span>{name}</span>
  {count && <span>{count}</span>}
  <span class="hidden md:block">{meta ?? ""}</span>
</div>
